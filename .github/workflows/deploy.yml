# ═══════════════════════════════════════════════════════════════
#  InfoKobuleti — Auto-Deploy to Hostinger via FTP
#  Triggers on push to main branch
#  
#  Required GitHub Secrets (Settings → Secrets → Actions):
#    FTP_SERVER    → Hostinger FTP hostname (e.g. ftp.infokobuleti.com)
#    FTP_USERNAME  → FTP account username
#    FTP_PASSWORD  → FTP account password
#    DB_HOST       → MySQL host (from Hostinger hPanel → Databases)
#    DB_NAME       → MySQL database name
#    DB_USER       → MySQL username
#    DB_PASSWORD   → MySQL password
# ═══════════════════════════════════════════════════════════════

name: 🚀 Deploy to Hostinger

on:
  push:
    branches:
      - main
  workflow_dispatch:   # Allow manual trigger from GitHub Actions tab

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest

    steps:
      # ─── 1. Checkout code ────────────────────────────────
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      # ─── 2. Inject production database config ────────────
      - name: 🔧 Write production config
        run: |
          php scripts/write-prod-config.php
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}

      # ─── 3. Deploy via lftp ───────────────────────────────
      - name: 🚀 FTP Deploy via lftp
        run: |
          sudo apt-get install -y lftp
          lftp <<EOF
          set ssl:verify-certificate no
          set ftp:ssl-allow yes
          set ftp:passive-mode yes
          open -u "${{ secrets.FTP_USERNAME }},${{ secrets.FTP_PASSWORD }}" "${{ secrets.FTP_SERVER }}"
          mirror -R -v \
            --exclude-glob .git/ \
            --exclude-glob .github/ \
            --exclude-glob scripts/ \
            --exclude-glob install/ \
            --exclude-glob cache/ \
            --exclude-glob uploads/ \
            --exclude-glob README.md \
            --exclude-glob DEPLOYMENT.md \
            --exclude-glob INSTRUCTIONS.md \
            --exclude-glob .gitignore \
            ./ ./
          bye
          EOF
