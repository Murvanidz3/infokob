# Placeholder workflow after repository reset (codebase removed for a fresh start).
# Restore FTP / DB injection steps when the new project exists again.
# Previous pattern: SamKirkland/FTP-Deploy-Action + secrets (FTP_*, DB_*) + scripts/write-db-config.php

name: CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ready for new pipeline
        run: |
          echo "Repository was reset to an empty state."
          echo "Replace this job with deploy/build steps when the new system is added."
